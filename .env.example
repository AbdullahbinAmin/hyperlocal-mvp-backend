# Environment
ENVIRONMENT=local

# Database Configuration
DB_USER=postgres
DB_PASS=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hyperlocal

# Security
# Generate a secure secret using: openssl rand -hex 32
JWT_SECRET=supersecretkeypleasechangeme
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
